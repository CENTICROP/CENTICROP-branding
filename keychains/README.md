`centricrop keychain one piece.stl` is meant to import as one piece and be easily painted in the slicer for multicolor printing.

Otherwise, you can import both `centricrop keychain logo.stl` and `centricrip keychain rectangle.stl` together as "one object" and paint or change filament colors as needed.